
const Tamplate = (title) => {
    return (
        <div class="panel panel-default">
        <div class="panel-heading">{title}</div>
        <div class="panel-body">
            Panel content
        </div>
    </div>
    )
};

export default Tamplate;